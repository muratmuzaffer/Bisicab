/** BisiCab marka renkleri — İzmir bisiklet taksi (siyah / sarı / yeşil). */

export const THEME = {
  /** Ana zemin (ink). */
  dark: '#0B0F0C',
  /** Koyu kart / panel. */
  surface: '#141A16',
  /** Birincil CTA (sarı). */
  brand: '#F5C518',
  /** Sarı hover / pressed. */
  brandDeep: '#D4A017',
  /** Koyu zeminde soft sarı vurgu. */
  brandLight: '#FFE566',
  /** Sürdürülebilirlik / başarı. */
  success: '#22C55E',
  /** Hata / iptal. */
  danger: '#EF4444',
  /** Açık liste / form zemini. */
  canvas: '#F7F8F6',
  /** Soft gövde metni (koyu zemin). */
  soft: '#E8EDE9',
} as const;
